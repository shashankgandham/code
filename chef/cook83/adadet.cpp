#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
	int t, n, x[1000005], h[1000005];
	scanf("%d", &t);
	while(t--) {
		scanf("%d", &n);
		for(int i = 1; i <= n; i++)
			scanf("%d%d", &x[i], &h[i]);
	
	}
	return 0;
}
